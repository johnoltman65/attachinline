# Schema for the configuration files of the attachinline module.

attachinline.settings:
  type: config_object
  label: 'Attach Inline Settings'
  mapping:
    csp-whitelist-method:
      type: string
      label: 'CSP Whitelist Method'
